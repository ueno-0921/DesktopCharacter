#pragma once

#include "STRING.hpp"
#include <windows.h>
#include <unordered_map>

#define DEFAULT TEXT("/:DEFAULT:/")

const STRING O_IMG = TEXT("--images");
const STRING O_IMG_I = TEXT("--images_index");
const STRING O_DEF_IMG_I = TEXT("--default_image_index");
const STRING O_SND = TEXT("--sounds");
const STRING O_SND_I = TEXT("--sounds_index");
const STRING O_INT_SND_I = TEXT("--interrupt_index");
const STRING O_CLICK_I = TEXT("--click_index");
const STRING O_IS_TRUE_RAND = TEXT("--is_true_rand");
const STRING O_VOL = TEXT("--volume");
const STRING O_POS = TEXT("--position");
const STRING O_IS_BASED_H = TEXT("--is_base_height");
const STRING O_MAX = TEXT("--max_size");
const STRING O_MIN = TEXT("--min_size");
const STRING O_INIT_SIZE = TEXT("--init_size");
const STRING O_SENSI = TEXT("--scale_sensi");
const STRING O_POS_PAD = TEXT("--position_pad");